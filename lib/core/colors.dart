import 'package:flutter/material.dart';

class AppColors {
  // 🌸 Primary soft pink
  static const Color primary = Color(0xFFFFA6C9); // flamingo pink

  // 🍑 Peachy accent
  static const Color accent = Color(0xFFFFD6E0);

  // 🤍 Background (cotton candy vibe)
  static const Color background = Color(0xFFFFF5F7);

  // 🧁 Card color
  static const Color card = Color(0xFFFFE4EC);

  // 🌷 Text colors (FIX for your theme error)
  static const Color textDark = Color(0xFF2E2E2E);
  static const Color textLight = Color(0xFFFFFFFF);
}